---
pagetitle: "Congressional Stock Trading Network"
page-layout: full
title-block-banner: false
toc: false
---

<style>
footer.footer {
  display: none !important;
}

.network-visual-shell {
  position: relative;
  width: 100%;
  height: calc(100vh - 64px);
  min-height: 680px;
  margin: 0;
  overflow: hidden;
  background: #f7f8fa;
}

.network-visual-shell iframe {
  position: relative;
  z-index: 2;
  display: block;
  width: 100%;
  height: 100%;
  border: 0;
  background: transparent;
  opacity: 0;
  transition: opacity 0.45s ease;
}

.network-visual-shell.is-loaded iframe {
  opacity: 1;
}

.network-loader {
  position: absolute;
  inset: 0;
  z-index: 1;
  display: grid;
  place-items: center;
  background:
    radial-gradient(circle at 50% 46%, rgba(214,181,74,0.08), transparent 28%),
    #f7f8fa;
  transition: opacity 0.35s ease, visibility 0.35s ease;
}

.network-visual-shell.is-loaded .network-loader {
  opacity: 0;
  visibility: hidden;
  pointer-events: none;
}

.loader-inner {
  display: grid;
  justify-items: center;
  gap: 18px;
  color: #444a52;
  font-size: 14px;
  font-weight: 700;
  letter-spacing: 0.01em;
}

.loader-network {
  position: relative;
  width: 116px;
  height: 78px;
}

.loader-line {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 58px;
  height: 1px;
  background: #c8cdd3;
  transform-origin: left center;
}

.loader-line.one { transform: rotate(-28deg); }
.loader-line.two { transform: rotate(31deg); }
.loader-line.three {
  width: 44px;
  transform: translate(-43px, 2px) rotate(0deg);
}

.loader-node {
  position: absolute;
  width: 13px;
  height: 13px;
  border-radius: 50%;
  box-shadow: 0 0 0 4px rgba(255,255,255,0.8);
  animation: networkPulse 1.8s ease-in-out infinite;
}

.loader-node.center {
  left: 51px;
  top: 33px;
  width: 16px;
  height: 16px;
  background: #d6b54a;
}

.loader-node.left {
  left: 9px;
  top: 55px;
  background: #2f6fdb;
  animation-delay: 0.18s;
}

.loader-node.right {
  right: 8px;
  top: 56px;
  background: #d64b4b;
  animation-delay: 0.36s;
}

.loader-node.top {
  right: 14px;
  top: 5px;
  border-radius: 3px;
  background: #667a70;
  animation-delay: 0.54s;
}

@keyframes networkPulse {
  0%, 100% { transform: scale(0.88); opacity: 0.62; }
  50% { transform: scale(1.2); opacity: 1; }
}

.network-quick-actions {
  position: absolute;
  right: 18px;
  bottom: 18px;
  z-index: 5;
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  justify-content: flex-end;
}

.network-quick-actions a {
  display: inline-block;
  padding: 9px 14px;
  border: 1px solid rgba(65,71,79,0.22);
  border-radius: 999px;
  background: rgba(255,255,255,0.94);
  color: #30343a;
  font-size: 13px;
  font-weight: 800;
  text-decoration: none;
  box-shadow: 0 5px 18px rgba(20,28,38,0.10);
  backdrop-filter: blur(8px);
}

.network-quick-actions a:hover {
  background: #fff;
  transform: translateY(-1px);
}

.network-writeup {
  width: min(980px, calc(100vw - 40px));
  margin: 0 auto;
  padding: 64px 0 90px;
}

.network-writeup h1 {
  margin: 0 0 10px;
  font-size: clamp(30px, 4vw, 48px);
  letter-spacing: -0.035em;
}

.network-writeup .lede {
  margin: 0 0 28px;
  color: #5f6670;
  font-size: 17px;
  line-height: 1.6;
}

.network-writeup h2 {
  margin-top: 38px;
}

.network-writeup p,
.network-writeup li {
  line-height: 1.65;
}

.network-link-row {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  margin: 24px 0 8px;
}

.network-link-row a {
  display: inline-block;
  padding: 8px 13px;
  border: 1px solid #cfd4da;
  border-radius: 9px;
  color: inherit;
  font-weight: 700;
  text-decoration: none;
}

.network-link-row a:hover {
  background: #f3f4f6;
}

@media (max-width: 700px) {
  .network-visual-shell {
    height: calc(100vh - 58px);
    min-height: 600px;
  }

  .network-quick-actions {
    right: 10px;
    bottom: 10px;
  }

  .network-quick-actions a {
    padding: 8px 11px;
    font-size: 12px;
  }
}
</style>

```{=html}
<div class="column-screen" style="margin:0;">
<div id="network-visual-shell" class="network-visual-shell">
<div id="network-loader" class="network-loader" aria-live="polite">
<div class="loader-inner">
<div class="loader-network" aria-hidden="true">
<span class="loader-line one"></span>
<span class="loader-line two"></span>
<span class="loader-line three"></span>
<span class="loader-node center"></span>
<span class="loader-node left"></span>
<span class="loader-node right"></span>
<span class="loader-node top"></span>
</div>
<div id="network-loader-message">Loading congressional stock data…</div>
</div>
</div>
<iframe id="congress-network-frame" src="https://strokeofluck.github.io/congressional-stock-network-2025/?embed=1" title="Interactive Congressional Stock Trading Network" loading="eager" allowfullscreen></iframe>
<div class="network-quick-actions">
<a href="#methodology">Methodology ↓</a>
<a href="https://strokeofluck.github.io/congressional-stock-network-2025/" target="_blank" rel="noopener">Open full screen ↗</a>
</div>
</div>
</div>
```

::: {#methodology .network-writeup}

# Congressional Stock Trading Network

<p class="lede">An interactive network connecting <strong>reported 2025 House public-stock purchases</strong> to politicians, companies, committees, and subcommittees.</p>

<div class="network-link-row">
<a href="https://strokeofluck.github.io/congressional-stock-network-2025/" target="_blank" rel="noopener">Open interactive network ↗</a>
<a href="https://github.com/StrokeOfLuck/congressional-stock-network-2025" target="_blank" rel="noopener">Source repository ↗</a>
<a href="stock-disclosures.qmd">Underlying disclosure project →</a>
</div>

## What it does

The opening graph focuses on stocks purchased by at least **two different politicians**, making shared purchases easier to see without treating the display filter as a data-quality rule.

Users can search politicians, stocks, committees, and subcommittees; inspect the original PTR records behind a stock; open committee and subcommittee drawers; export institution-level CSV summaries; copy links to selected drill-down states; and compare the graph with a broader House coverage panel.

## Transaction-date committee matching

Committee assignments are not treated as one fixed roster for the whole year.

The project uses pinned historical snapshots from the `unitedstates/congress-legislators` dataset and matches committee membership to the transaction date.

- **Jan. 1–2, 2025:** Dec. 17, 2024 snapshot, because those dates are still in the 118th Congress.
- **Jan. 3–20, 2025:** Jan. 21, 2025, the first usable House committee snapshot for the 119th Congress.
- **Jan. 21 onward:** latest available snapshot on or before the transaction date.

A missing committee match does **not** remove a stock purchase. Committee data are added after the accepted stock-purchase dataset is created.

## Coverage checks

The project compares accepted purchasers with the 119th-Congress House roster and separates:

1. politicians in the main shared-stock graph;
2. accepted purchasers hidden only because none of their tickers pass the 2+ politician display rule;
3. House members for whom the project found no accepted 2025 public-stock purchase.

The third group is a **coverage-review category**, not proof that someone did not trade.

## Why I built it

I wanted a way to move beyond a flat disclosure table and explore how reported stock purchases intersect with congressional institutions.

The result combines public-records cleaning, network visualization, historical committee metadata, and reproducibility checks in one interactive project.

The interactive build and analysis source are maintained in a **standalone repository** rather than inside the portfolio repository. That keeps the portfolio site lightweight and keeps the network's analysis and deployment history independent from the website.

:::

<script>
(function () {
  const shell = document.getElementById("network-visual-shell");
  const frame = document.getElementById("congress-network-frame");
  const message = document.getElementById("network-loader-message");

  if (!shell || !frame) return;

  let finished = false;
  const startedAt = performance.now();
  const minimumLoaderMs = 1200;

  function revealNetwork() {
    if (finished) return;
    finished = true;

    const elapsed = performance.now() - startedAt;
    const remaining = Math.max(0, minimumLoaderMs - elapsed);

    window.setTimeout(function () {
      shell.classList.add("is-loaded");
    }, remaining);
  }

  frame.addEventListener("load", function () {
    if (message) {
      message.textContent = "Arranging politicians, stocks, and committees…";
    }
  });

  window.addEventListener("message", function (event) {
    if (event.origin !== window.location.origin) return;

    if (
      event.data &&
      event.data.type === "congress-network-ready"
    ) {
      if (message) {
        message.textContent = "Network ready";
      }
      revealNetwork();
    }
  });

  /* Safety fallback in case a browser suppresses the ready message. */
  window.setTimeout(function () {
    if (!finished) {
      if (message) {
        message.textContent = "Opening network…";
      }
      revealNetwork();
    }
  }, 20000);
})();
</script>
